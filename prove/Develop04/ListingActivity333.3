

public class ListingActivity : Activity
{
    public ListingActivity()
    {
        _name = "ListingActivity";
        _description = "Escuchar";
    }
    public void Run()
    {
        DisplayStartingMessage();
        _duration = (1000);
        //user breath in 5 seconds
        //UserStringHandle breath out 5 seconds
        //repeat duration...

        DateTime startTime = DateTime.Now;
        Console.WriteLine("Breath in");
        ShowCountDown(5);

        DateTime stopTime = startTime.AddSeconds(_duration);
        Console.WriteLine("Breath out");
        ShowCountDown(5);

        DisplayEndingMessage();

        //do{

        //}
        ///while (DateTime.Now < stopTime);

    }
    
   
}

//leer de los text, System.IO.StreamWriter
//string filename = "myFile.txt";
//string[] lines = System.IO.File.ReadAllLines(filename);

//foreach (string line in lines)
//{
   // string[] parts = line.Split(",");

    //string firstName = parts[0];
    //string lastName = parts[1];
//}
//Otro ejemplo:  Don't forget to put this at the top, so C# knows where to find the StreamWriter class
using System.IO; 

//...

//string fileName = "myFile.txt";

//using (StreamWriter outputFile = new StreamWriter(filename))
//{
    // You can add text to the file with the WriteLine method
    //outputFile.WriteLine("This will be the first line in the file.");
    
    // You can use the $ and include variables just like with Console.WriteLine
    //string color = "Blue";
    //outputFile.WriteLine($"My favorite color is {color}");
//}